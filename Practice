wait i forgot you to sent roueter.py

from fastapi import APIRouter

from handler import database_filler_start

router = APIRouter()


@router.post(

    "/data_filler_mcp",

    operation_id="data_filler_mcp"

)

async def data_filler_mcp(payload: dict):

    try:

        result = payload.get("result")

        response = await database_filler_start(result)

        return {

            "jsonrpc":"2.0",

            "id":payload.get("id"),

            "result":response

        }

    except Exception as e:

        return {

            "jsonrpc":"2.0",

            "id":payload.get("id"),

            "error":str(e)

        }
 
